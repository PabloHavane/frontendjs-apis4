// Import CSS global
import './assets/style.css';

// Import fonctions principales ou contrôleurs

// Exemple d'utilisation :
// Dès que le DOM est chargé, tu appelles les fonctions nécessaires
document.addEventListener('DOMContentLoaded', () => {
    // Vérifie si la page contient un élément spécifique avant d'exécuter


    // if (document.querySelector('#liste-matchs')) {
    //     chargerMatchs();
    // }
});
